<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <%= link_to "淘书网", root_path, class: "navbar-brand" %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav pull-right">
                <li><%= link_to "首页", root_path %></li>
                <li><%= link_to "所有书籍", books_path %>
                <li><%= link_to "所有书单", booklists_path %>
                <% if signed_in? %>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <%= current_user.name %>
                            <span class="caret">
                            </span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "个人主页", current_user %></li>
                            <li><%= link_to "个人设置", edit_user_path(current_user) %></li>
                            <li role="separator" class="divider"></li>
                            <li><%= link_to "登出", signout_path, method: "delete" %></li>
                        </ul>
                    </li>
                <% else %>
                    <li><%= link_to "登录", signin_path %></li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
