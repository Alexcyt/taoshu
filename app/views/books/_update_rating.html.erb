<%= form_for(current_user.ratings.find_by(book_id: @book.id), html: { method: :patch }, remote: true) do |f| %>
    <%= f.hidden_field :score, value: current_user.my_rating(@book) %>
    <%= f.submit "更新评分", id: "rating_button", class: "btn btn-small btn-success", disabled: true %>
<% end %>
